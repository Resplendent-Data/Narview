# Narview Contracts

This package holds cross-client fixtures and schema notes shared by Narview clients.

The desktop client remains TypeScript-first and the mobile client is Dart-first. Shared
behavior should be proven through JSON fixtures before introducing generated schemas.

