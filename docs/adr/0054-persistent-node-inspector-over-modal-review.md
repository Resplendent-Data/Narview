# Persistent node inspector over modal review

Selecting an **Attention Node** should show its diff, context, **Review Threads**, and review actions in a persistent inspector within the **Guided Review Workspace** rather than opening a modal. Reviewers need to move repeatedly through many nodes, so the interface should preserve map orientation and avoid a constant open-close loop.
