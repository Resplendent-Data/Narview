# Context expansion and full-file view without code intelligence

Narview lets users expand surrounding diff context and fetch the whole file when reviewing a thread or file change. Symbol navigation such as command-clicking a function to jump to its definition is deferred beyond v1 because it requires code intelligence that is deeper than remote diff review.
