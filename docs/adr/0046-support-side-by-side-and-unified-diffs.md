# Support side-by-side and unified diffs

Narview supports both side-by-side and unified diff views in v1, with the user's preferred mode remembered locally. Unified diffs support compact queue-driven review, while side-by-side diffs support careful inspection of specific file changes.
