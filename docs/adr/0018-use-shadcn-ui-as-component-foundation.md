# Use shadcn/ui as the component foundation

Narview uses shadcn/ui and Radix primitives as the React component foundation, while customizing density, layout, and review-specific components for a desktop code review workflow. This provides accessible UI building blocks without treating shadcn's default SaaS aesthetic as the product's visual identity.
