# Key reviewed state by GitHub review thread ID

Narview keys local **Reviewed** state by the GitHub review thread GraphQL ID, while storing repository, pull request, path, line context, author, and creation metadata as recovery context. This keeps **Reviewed** attached to the thread a person inspected even when later commits move or invalidate the original diff position.
