# Local redacted operational logs

Narview keeps local operational logs for debugging, but logs must avoid raw code, diff hunks, review comment bodies, OAuth tokens, and sensitive request details. Diagnostics export should be explicit and previewable so users control what leaves their machine.
