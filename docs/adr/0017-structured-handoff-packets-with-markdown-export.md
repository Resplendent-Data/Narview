# Structured handoff packets with Markdown export

Narview builds **Handoff Packets** from a structured internal model and can export them as Markdown for human-friendly copying. This keeps agent integrations and thread identity precise while still making the first workflow easy to paste into external AI tools.
