# Attention map as primary review overview

Narview should open a pull request into an **Attention Map** as the primary **Review Overview**, with an ordered **Review Path** visible alongside it. The map gives reviewers a global picture of how changed areas relate to one another, while the Review Path keeps keyboard-driven review concrete by moving focus between **Attention Nodes** instead of leaving the reviewer to wander an open canvas.
