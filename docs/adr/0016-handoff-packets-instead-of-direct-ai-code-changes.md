# Handoff packets instead of direct AI code changes

Narview does not directly apply AI-generated code changes in v1. It can prepare **Handoff Packets** with selected pull request context for external coding agents, preserving Narview's remote-first review focus while still improving AI-assisted fix workflows.
