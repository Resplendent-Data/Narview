# Attention map is the single primary review surface

Narview should make the **Attention Map** and **Review Path** the single primary pull request review surface instead of maintaining a separate thread-only mode as a parallel workflow. **Review Threads** should be integrated into relevant **Review Targets**, while filtered thread views may remain supporting controls inside the same workspace if real usage shows a need for faster thread triage.
