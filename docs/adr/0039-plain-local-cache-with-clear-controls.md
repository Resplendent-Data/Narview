# Plain local cache with clear controls

Narview stores cached pull request metadata and diff content plainly in the local app data directory in v1, while keeping OAuth tokens in OS secure storage. The app should provide clear controls to remove cached review data without deleting durable local review states unless the user explicitly chooses to do so.
