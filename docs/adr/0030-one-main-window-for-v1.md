# One main window for v1

Narview uses one main desktop window in v1 rather than supporting multiple simultaneous app windows. This keeps review session restoration, local state updates, bulk actions, and pull request switching simpler while the core review workflow is still being shaped.
