# Global hotspot defaults with repository overrides

Narview uses global defaults for generated-file suppression and hotspot scoring, while allowing each saved repository to override those rules. This keeps the first experience useful out of the box without forcing unrelated repositories to share one interpretation of generated files, vendor paths, or important contracts.
