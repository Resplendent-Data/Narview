# Guided Review Workspace layout

Narview uses an adaptive **Guided Review Workspace** with a left review map, center review canvas, and right inspector, plus a focus mode for deep diff reading. The default review order is guided by **Review Queues** and **Hotspots** rather than alphabetical file order, while preserving an all-files escape hatch.
