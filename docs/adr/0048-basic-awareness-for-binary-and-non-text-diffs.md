# Basic awareness for binary and non-text diffs

Narview shows binary, image, and non-text file changes as changed files in v1, but does not provide rich previews or specialized diffing for them. Users can open those files in GitHub when they need provider-native handling.
