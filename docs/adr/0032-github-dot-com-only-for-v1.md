# github.com only for v1

Narview supports github.com in v1 and does not support GitHub Enterprise Server. The GitHub client should still avoid unnecessary hardcoding so Enterprise support can be evaluated later, but custom enterprise auth, API hosts, certificates, and version differences are outside the first release.
