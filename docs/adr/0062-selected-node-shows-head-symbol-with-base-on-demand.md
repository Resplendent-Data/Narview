# Selected node shows head symbol with base on demand

The selected **Attention Node** review content should lead with the changed context and current head version of the enclosing symbol. The base version should be available on demand rather than shown as the default primary view, because reviewers usually need to understand the new logic first while still being able to compare old behavior when necessary.
