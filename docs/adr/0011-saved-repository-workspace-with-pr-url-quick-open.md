# Saved repository workspace with PR URL quick-open

Narview shows pull requests from repositories the user explicitly saves into a local **Workspace**, while still allowing quick-open by GitHub pull request URL. This avoids scanning every repository a token can access while keeping ad hoc review of a specific pull request fast.
